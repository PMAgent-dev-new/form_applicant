# 目標設計: LIFT JOB応募Baseの生年月日・希望勤務地

- 対象クライアント: 株式会社PM Agent（RIDE JOB / LIFT JOB）
- 目的: LIFT JOB応募をLark Baseへ保存する際、生年月日を年齢計算式が解釈できる `YYYY/MM/DD` 形式に統一し、希望勤務地の日本語ラベルを維持する。
- 完了条件:
  - `19960101` が通知・Base payload・直接Base書き込みで `1996/01/01` になるテストが通る。
  - 希望勤務地 `東京` がBaseの `希望勤務地` 列へそのまま入るテストが通る。
  - 対象テスト、型チェック、lint、buildが成功する。
  - main反映後、本番ヘルスチェックと本番E2Eを行い、Baseの保存値 `生年月日`、算出された `年齢`、`希望勤務地` の3点を読み戻す。
- スコープ外: Lark Baseの年齢計算式そのものの変更、過去応募者データの補完、応募者への通知内容変更（表記正規化を除く）。
- 戻し方: 本変更のマージコミットをrevertし、Vercelの直前の本番デプロイへ戻す。
- 実行モデル: Astra（Codex本体）で実装・検証。クライアント資産への反映前に読み取り専用Reviewer 2本で反証レビュー。

## 判断ログ

- 2026-09-18 Astra判断: フォーム入力値は8桁数字のまま保持し、外部へ渡す境界（通知・Base payload）で日付表記を正規化する。入力UIとバリデーションの仕様変更を避け、影響範囲をLark連携に限定するため。
- 2026-09-18 Astra判断: 希望勤務地は現行実装ですでに日本語ラベルへ変換されているため、挙動を変えず回帰テストを強化する。
- 2026-09-18 Astra判断: Baseの計算エラー再発を防ぐため、不正な生年月日は元値を保存せず、サーバー境界で外部送信前にHTTP 400として拒否する。

## 保留

- なし。

## 仮決め

- なし。

## 検証結果

- `npm test`: 15ファイル・243テスト成功、post-deploy guard 9テスト成功。
- `npm run typecheck`: 成功。
- `npm run lint`: エラー0。既存ファイルの未使用変数警告5件のみ。
- `npm run build`: 成功。最初の実行はworktree外を指す `node_modules` シンボリックリンクをTurbopackが拒否したため失敗し、worktree内で `npm ci` 後に再実行して成功。
- Reviewer（事実確認）: 不正日付を元値のまま保存する点を中指摘。サーバー側で外部送信前に400拒否するよう修正。
- Reviewer（抜け漏れ・影響）: 本番で年齢計算結果まで読み戻す点を中指摘、区切り欠落・混在の許容を低指摘。完了条件を3項目の読み戻しに具体化し、許容形式を8桁または同一区切りに限定。
- Reviewer再確認: 上記指摘は解消済み、新たなコード上の問題なし。検証件数の記録を243件へ更新後、記載も再確認済み。
- 混入チェック: `src/app/api/coupang/applicants` と `docs` を対象に、PM Agent／RIDE JOB／LIFT JOB／Coupangの名称を除外して実行し、いずれも検出0件。
