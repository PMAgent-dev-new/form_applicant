# 目標設計: LIFT JOB 広告通知・Base反映の完全化（2026-09-16）

## 対象クライアント

- 株式会社PM Agent
- 対象サービス: LIFT JOB

## 目標

Meta広告とChatGPT広告からのLIFT JOB応募について、Lark通知へ媒体・キャンペーン等を表示し、LIFT JOB Baseへ同じ経路情報を欠落なく保存する。ChatGPT広告は `oppref` だけの着地でも広告流入として識別し、OpenAI Conversions APIへ応募完了を返す。

## 完了条件

1. Meta広告の応募を想定した入力で、Lark通知が配置名を含む広告表記になり、Baseで `media_name=Meta広告` とUTM・広告ID群を確認できる。
2. ChatGPT広告の応募を想定した入力で、Lark通知が `ChatGPT広告（cpc）` になり、Baseで `media_name=ChatGPT広告`、`application_source=openai(ad)`、UTMを確認できる。クリック識別子 `oppref` はBaseへ複製せず、OpenAIへの成果返却だけに使う。
3. `oppref` だけの着地でも `utm_source=openai` / `utm_medium=cpc` として扱い、自然流入へ誤分類しない。
4. LIFT JOB応募APIが `oppref` をOpenAI Conversions APIへ渡す。`oppref` がない応募は送信しない。
5. LIFT JOB Baseに必要な受け皿が存在し、型・選択肢・フィールド名をAPIで読み戻して確認できる。
6. ローカルで `npm test`、`npm run typecheck`、`npm run build` が成功する。
7. main反映後、`ridejob-entry` と `ridejob-form` の最新productionが対象Git SHAでREADYになり、認証付きhealthが `ready` を返す。
8. 個人情報を含まないMeta/ChatGPTテストを各1件だけ本番経路へ送り、Lark Webhook受理、Base実レコードの値、OpenAIのvalidate-only応答を実測する。テストBaseレコードは作成時のrecord IDを保持してID指定で削除し、同じsubmission IDが0件であることを確認する。
9. 応募単位の安定した `submission_id` でBaseをupsertし、ブラウザ再送時の重複レコードを防ぐ。Meta/OpenAIのevent IDにも同じ値を使う。

## スコープ外

- 実在応募者データの変更
- 広告予算、配信ON/OFF、クリエイティブの変更
- Larkの新規Baseや外部サービスの新規リソース作成
- OpenAI AdsのAdvanced Matching有効化（第三者への個人データ提供を伴うため、既定OFFを維持）

## 戻し方

- コードは本変更コミットをrevertし、直前のREADYなproductionへrollbackする。
- Baseへ追加した選択肢・列は変更前スキーマを記録し、本変更で作った未使用項目だけを対象に戻す。既存応募レコードは変更しない。
- 本番テストは一意な `LIFT-E2E-20260916-*` を使い、読み戻し後にテストレコードだけを削除する。

## 判断ログ

- 2026-09-16／ユーザー指示: 「進めて」。直前に提示したBase受け皿、ChatGPT広告計測、本番E2Eの残対応を実行する。
- 2026-09-16／Astra判断: `oppref` はOpenAI広告クリックにだけ付く識別子なので、同一リクエストのqueryに `oppref` がありUTMがない場合は `openai/cpc` とする。過去Cookieの `oppref` だけでは新しい自然流入を上書きしない。
- 2026-09-16／Astra判断: OpenAI Advanced Matchingは有効化せず、`oppref` のみを照合キーとして送る。
- 2026-09-16／Astra判断: 本番テストはメール・SMSを発生させず、通知本文とBase上でテストと判別できる非個人データを使用する。
- 2026-09-16／Reviewer指摘を一次コード・本番Baseで再検証: 現行LIFT JOBルートに `oppref` とOpenAI CAPIが未配線、Base Automationは実保存を同期保証せず、共通resolver変更は全フォームへ波及することを確認した。
- 2026-09-16／Astra判断: LIFT JOB Baseは既存の専用アプリ資格情報とBitable APIで直接upsertし、成功時は旧Automation Webhookを呼ばない。直接書き込み未設定時だけ既存Webhookへフォールバックする。
- 2026-09-16／Astra判断: `oppref` からの `openai/cpc` 補完はLIFT JOBフォーム内だけに置き、共有resolverとjobmadley互換Cookieの意味は変えない。
- 2026-09-16／Astra判断: `oppref` はBaseへ保存しない。OpenAI CAPIは `oppref` を必須とし、Advanced MatchingがONでもクリック識別子なしでは送信しない。
- 2026-09-16／Astra判断: 本番E2Eはhealth tokenで認証したtest modeを使う。メール・SMS・Meta CAPIを抑止し、OpenAI CAPIは `validate_only=true` にして本番コンバージョンへ計上しない。
- 2026-09-16／Astra判断: Baseスキーマを先に後方互換で追加し、API読み戻し後にコードをデプロイする。rollback時も列は即削除せず、利用0件を確認してから撤去する。
- 2026-09-16／Reviewer指摘を一次コードで再検証: Cookieの古い`oppref`が別媒体応募へ渡る、ChatGPT応募でもMeta Leadが発火する、検索→作成の同時実行で重複余地がある、通知済みフラグ更新失敗が500再送を誘発する問題を確認した。
- 2026-09-16／Astra判断: OpenAI広告として解決した応募だけに`oppref`を渡し、Meta LeadはMeta広告流入だけに限定する。Base作成は決定的UUIDの`client_token`で冪等化し、通知受理後のフラグ更新失敗はログ・失敗一覧へ残すが応募APIは成功にする。
- 2026-09-16／本番ガードの実測: `ridejob-form`には過去のOpenAI sensitive値が未配置で、Vercelは既存sensitive値を再表示しないため、認証付きhealthが不足を検知して旧版へ自動rollbackした。OpenAIの新規キー発行は行わず、資格情報を持つ`ridejob-entry`へ非個人情報（event ID・oppref・URL・時刻）だけを送る内部relayを採用する。
- 2026-09-16／Astra判断: relayは専用のランダムトークンで認証し、受信側は直接資格情報がない場合に再relayしない。メール・電話・IP・User-Agentはrelay payloadへ含めず、Advanced Matching既定OFFを維持する。
- 2026-09-16／Reviewer指摘を一次コードで再検証: 受信側token欠落やrelay先障害をhealthが見逃す、relayが一過性障害を再試行しない、`sourceUrl` queryに任意情報が混ざる余地を確認した。受信側tokenをreadiness必須化し、relay GETの実到達・直接資格情報をhealthから検査、429/5xx/通信例外を同一event IDで1回再試行、relayのURLをorigin+pathnameへ限定した。
- 2026-09-16／本番E2Eの実測: `ridejob-form`は最新deploymentがREADYでも、直前rollback後のカスタムドメインaliasが旧commitを指したままだった。最新deploymentを手動promoteし、デプロイガードへ「実ドメインaliasと最新production URLの一致」を追加した。healthだけでは旧版もreadyを返せるため、SHA・alias・healthの3点を別々に検査する。
- 2026-09-16／Reviewer指摘を一次コードで再検証: alias切替の伝播遅延を即時失敗にする点と、health失敗後からrollback直前までのalias手動変更を再検査しない競合を確認した。alias一致は最大6回・10秒間隔で待ち、rollback直前にも同じalias invariantを再検証する。
- 2026-09-16／スクリーンショットの一次確認: 「浦田 智美」は同じLarkチャットに届いたIndeed応募（求人: 総務・労務事務スタッフ、企業: 株式会社青沼総合事務所）であり、LIFT JOB広告応募ではなかった。LIFT JOB Baseを氏名で検索して0件、通知元と警告元は同じ別アプリだったため、本変更のLIFT JOBフォーム未反映ではない。

## 保留

- なし。LIFT JOB Baseのapp token、table ID、読み書き・削除権限は2026-09-16にBitable APIで実測済み。

## 仮決めした前提

- ChatGPT広告の標準入稿URLは `utm_source=openai&utm_medium=cpc` とする。UTMが欠落してもqueryの `oppref` があれば同じ分類へ復元する。
- Metaの配置別sourceは `meta` / `fb` / `ig` / `th` / `msg` の既存定義を維持する。

## 実行モデル

- Astra: Codexメインセッション（設計、実装、外部変更、git、検証）
- Reviewer相当: `codex exec -s read-only` を事実確認と抜け漏れ・影響の2観点で実行

## 実測結果

- 2026-09-16: 本番LIFT JOB Baseへ15項目を追加し、総項目数89をAPIで読み戻した。`応募経由(マスタ連動)` へ `fb(ad)` / `meta(ad)` / `msg(ad)` / `openai(ad)` を追加し、既存レコードは変更していない。
- 2026-09-16: LIFT JOB専用の直接upsert、通知重複防止、Meta / ChatGPT分類、OpenAI validate-only・内部relay、認証なしtest mode拒否、production alias検査を含む全217 Vitestと全9 post-deploy guardテストが成功した。lintはエラー0（既存警告5件）、TypeScript型検査、production build、`git diff --check`も成功した。
- 2026-09-16: `ridejob-entry` / `ridejob-form` のproductionへLIFT JOB専用Base接続5変数を配置し、両方で変数名を読み戻した。値はログへ出していない。
- 2026-09-16: PR #86（commit `4316030`）とPR #87（commit `c4e2744`）をmainへ反映した。`ridejob-entry` / `ridejob-form` は双方ともcommit `c4e2744`のREADY deploymentへ切替え、実ドメインaliasを確認した。認証付きhealthは双方HTTP 200 `ready`、post-deploy guardとCIも成功した。
- 2026-09-16: Meta E2Eを`ridejob-entry`へ1件送信し、Lark実メッセージでInstagram広告、キャンペーン、広告セット、CR、広告ID、広告名、LPを読み戻した。Baseでは`Meta広告` / `ig(ad)`、全UTM、LP、submission ID、通知済み=trueを確認した。
- 2026-09-16: ChatGPT E2Eを`ridejob-form`へ1件送信し、内部relay経由のOpenAI `validate_only`を含め失敗0件でHTTP 200。Lark実メッセージで`ChatGPT広告（cpc）`と全経路項目を読み戻し、Baseでは`ChatGPT広告` / `openai(ad)`、全UTM、LP、submission ID、通知済み=trueを確認した。Baseと通知のLPに`oppref`がないことも確認した。
- 2026-09-16: E2E Baseレコード2件はrecord ID指定で削除し、使用した4つのsubmission IDが全て0件になったことを再検索した。LarkのE2E通知4件は受信アプリが送信者・管理者ではないためAPI取消権限がなく、チャット上に残っている（氏名はE2E表記、連絡先は無効値）。
- 2026-09-16: PR #88をmainへ反映（commit `51ba297`）。両projectで最新production SHA、実ドメインaliasのdeployment URL、認証付きhealth `ready` が一致し、強化後post-deploy guardとCIが成功した。コード・Base・環境変数・実ドメインの本番反映は完了。
